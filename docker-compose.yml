services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nisa_world_backend
    environment:
      DATABASE_URL: postgres://c77d173b2cc4a015abecbad44fcd2bf75a5a13a35c98fc0d212f3c46d6b3c271:sk_ej3VJPZTBdMG3d8Xonuox@db.prisma.io:5432/postgres?sslmode=require&keepalives=1&keepalives_idle=5&keepalives_interval=1&keepalives_count=2&connection_limit=5
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-your-secret-key-change-in-production}
      JWT_ACCESS_TOKEN_EXPIRE_HOURS: ${JWT_ACCESS_TOKEN_EXPIRE_HOURS:-24}
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    networks:
      - nisa_network
    restart: unless-stopped

networks:
  nisa_network:
    driver: bridge
